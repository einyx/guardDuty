# Local .terraform directories
**/.terraform/*
**/.terragrunt-cache/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Ignore any tfvars file, except the ones we care about
!terraform.tfvars
!region.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in

override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
*tfplan*

# Don't incluste the terragrunt cache
.terragrunt-cache/
